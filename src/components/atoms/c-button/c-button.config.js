
/**
 * Used to sync prop validation (in components) with argTypes (in stories)
 */
export const colorOptions = ['primary', 'secondary', 'contrast']

// Fix broken dev tools on linux
export default {}
